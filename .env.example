# Discord Bot Configuration
DISCORD_TOKEN=your_bot_token_here
DISCORD_CLIENT_ID=your_application_client_id_here

# AWS S3 Configuration (for image storage)
AWS_S3_BUCKET=your-s3-bucket-name
AWS_REGION=us-east-1
CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net

# Production CDN Build (optional)
# Set PUBLIC_USE_CDN=true to serve images from CloudFront CDN
# PUBLIC_USE_CDN=true
# PUBLIC_CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net

# Option 1: Use AWS profile (recommended for local development)
# AWS_PROFILE=srefs

# Option 2: Use explicit credentials
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key